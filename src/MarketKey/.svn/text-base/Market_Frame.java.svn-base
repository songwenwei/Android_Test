//测试点：市场
package MarketKey;

import com.robotium.solo.Solo;
import StockMasterBase.MarketStockActivity;
import StockMasterBase.StockActivity;
import android.view.ViewGroup;
import android.widget.TextView;

//其他/市场行情框架
public class Market_Frame extends StockActivity {

	// 点击【搜索】
	public void testCase001() throws Exception {
		CaseName = "点击【搜索】：无法校验";
		CaseCheck = this.EqualsChecked(false);
		System.out.println(CaseCheck);

	}

	// 切换TAB栏按钮展示
	public void testCase002() throws Exception {
		CaseName = "切换TAB栏按钮展示：无法校验";
		CaseCheck = this.EqualsChecked(false);
		System.out.println(CaseCheck);

	}

	// 行情列表本地缓存，覆盖安装校验缓存--android
	public void testCase003() throws Exception {
		CaseName = "行情列表本地缓存，覆盖安装校验缓存--android：无法校验";
		CaseCheck = this.EqualsChecked(false);
		System.out.println(CaseCheck);

	}

}